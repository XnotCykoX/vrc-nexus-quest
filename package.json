{
  "name": "vrc-nexus-quest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VRC-NEXUS — standalone Meta Quest companion (avatars, favourites, discover). Sideload via SideQuest.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "cap:add": "cap add android",
    "cap:sync": "vite build && cap sync android",
    "cap:open": "cap open android",
    "apk": "vite build && cap sync android && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/filesystem": "^6.0.3",
    "@capacitor/preferences": "^6.0.3",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^5.4.11"
  }
}
